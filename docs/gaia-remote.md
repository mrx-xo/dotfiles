# gaia-remote → moved

The GAIA TV remote (and the whole GAIA application layer) graduated to its own
repo: **github.com/Marx-A00/gaia** (private), dev checkout at
`~/roaming/projects/gaia`, deployed clone on homelab at `~/gaia`
(`gaia-control.service`, port 8092).

Everything that lived here — gaia_control.py, the mock lineage, the design
README — moved there intact (`mocks/` → `remote/web/`).
